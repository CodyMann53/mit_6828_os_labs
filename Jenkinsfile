node {
    stage('Setup') {
        println("Setup stage!")
    }
    stage("Build") {
        println("Build stage!")
    }
    stage("Test") {
        println("Test Stage!")
    }
    stage("Analyze"){
        println("Analyze Stage!")
    }
}